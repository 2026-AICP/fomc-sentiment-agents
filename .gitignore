__pycache__/
*.pyc
.DS_Store
*.db
data/raw/
data/text/
reports/out/
reports/agent_out/
logs/
.venv/
.pytest_cache/

# 모델 가중치 (419MB) — git 에 올리지 않음. 드롭박스에서 받아 로컬 배치.
models/

# 대용량 데이터·생성물 — git 에 올리지 않음 (드롭박스/로컬 관리)
# (주의: .gitignore 는 인라인 주석 불가 → 패턴만 한 줄에)
data/wsj/
data/news/
outputs/
*.pkl
# data/statements/ 는 git 에 포함(공유·재현성) — 성명문은 작은 텍스트(~370KB). WSJ·모델만 제외.

# API 키·비밀 — 절대 커밋 금지 (오타·백업·변형 사본까지 글롭으로 방어: .fredapi_ke, .fredapi_key.bak 등)
.newsapi*
.fredapi*
*.key
.env


# 웹 대시보드 (dashboard-web) — 소스만 커밋, 빌드·의존성·데이터는 재생성
dashboard-web/node_modules/
dashboard-web/dist/
dashboard-web/public/data/

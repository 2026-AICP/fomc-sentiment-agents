__pycache__/
*.pyc
.DS_Store
*.db
data/raw/
data/text/
reports/out/
.venv/
.pytest_cache/

# 모델 가중치 (419MB) — git 에 올리지 않음. 드롭박스에서 받아 로컬 배치.
models/

# 대용량 데이터·생성물 — git 에 올리지 않음 (드롭박스/로컬 관리)
data/wsj/         # WSJ 원본 CSV (드롭박스)
data/statements/  # FOMC 성명문 원본 (드롭박스)
outputs/          # 생성된 인덱스·그림·표
*.pkl             # 중간 산출물 (기사별 점수 등)


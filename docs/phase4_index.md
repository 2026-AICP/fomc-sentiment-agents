# Phase 4 — 인덱스 집계 방식 비교

> 2000~2025 성명문 214회의 인덱스로 작년 방식(label_avg)과 새 방식(conf_weighted) 비교.
> ※ 아래 수치는 원본 FinBERT(T=1) 전환 이전 산출 — 파이프라인 재실행 시 갱신 필요.

## 1. 작년 방식 vs 새 방식 (Phase 4 합격선)

| | label_avg (작년) | conf_weighted (새 방식) |
|---|---|---|
| 정의 | argmax 라벨(-1/0/1) 평균 | score를 확신도(1−정규화 엔트로피)로 가중 |
| 표준편차 | 0.251 | 0.157 |
| 상관 | — | +0.944 (대체로 같이 움직임) |

**핵심 발견**: label_avg가 거의 0(긍·부정 라벨 상쇄)인 회의 **19건 중 11건에서, conf_weighted는 0이 아닌 신호를 잡았다.**

→ 작년 방식은 "긍정 문장 2개, 부정 문장 2개"면 무조건 0으로 뭉개지만, 확신도 가중은 **"모델이 어느 쪽을 더 확신했나"**를 반영해 미세 신호를 살린다. 이것이 작년 대비 인덱스 개선점이다.

## 2. 결론

- 새 집계(conf_weighted)는 작년 방식이 놓치는 신호를 잡는다(19건 중 11건).
- 이 개선은 작년 대비 인덱스 신뢰도를 높이는 방향이다.

## 3. 산출물
- `outputs/figures/phase4_methods_compare.png` — 방식 비교

## 4. 한계·다음
- 감성 엔진은 **원본 FinBERT(T=1)** — 자체 라벨 기반 온도보정은 지도교수 피드백(2026-07)으로 제외.
- 다음: 성명문·회의록·기자회견을 **각각** 집계 후 가중평균(통합분석 아님).

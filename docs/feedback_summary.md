# 피드백 반영 현황

2026-08-26 / main `955845c` / deploy `846de23`
상세는 `docs/feedback_status.md`

## 교수님 (7월)

| 항목 | 상태 |
|---|---|
| 자체 라벨링 제거 · 원본 FinBERT(T=1) 복귀 | 완료 |
| 뉴스 키워드 F∧M 세트 적용 (WSJ 수집과 동일) | 완료 |
| 회의록 축 추가 (2000~2026, 214회의) | 완료 |
| 성명문/회의록/기자회견 분리 분석 | 완료 |
| 크롤링 금지 → API 전환 | 완료 |
| 시장 비교·VIX 보류 | 유지 중 |
| 키워드 조정 여부 | 여쭤봐야 함 |
| 설명력 분석 | 미착수 |

## 조교님 (8월)

| 항목 | 상태 |
|---|---|
| 질문5 — 표본 적은 날 신호 게이팅 | 완료 (PR #4, 지현민) |
| 질문6 — 회의록 3주 지연 → 속보치/확정판 분리 | 완료 (PR #4, 지현민) |
| 신뢰도 UI 3단계(높음·보통·낮음) + 상세보기 | 완료 |
| 과거 실시간 값 덮어쓰기 금지 | 완료 (vintage + 속보치/확정판) |
| 탈락 기사 감사 | 1차만 완료 — 아래 참고 |
| 알림 기능 확장 | 미착수 |
| 분석 설계 (다항 로지스틱 등) | 미착수 |

## 솔직하게 덜 된 것

**탈락 기사 감사** — 1차는 33건 표본, 놓침률 27.3%. FOMC 회의록 보도 4건 전부 탈락.
재감사가 필요한데 그동안 못 한 이유: 워크플로 설정 누락으로 **탈락 기사가 저장이 안 되고 있었음**
(하루 164건씩 유실). 8/26에 고침. 지금부터 쌓임.

**신뢰도 임계값 (기사 15건 · CI 0.60)** — 데이터로 정한 값 아님. 코드에도 "잠정"이라고 적혀 있음.
다른 신호 임계값(θ)은 분위수로 보정했는데(`signal_calibration.md`) 이것만 안 됨.
수집이 깨져 있던 기간이 표본에 섞여서 지금 보정하면 안 됨. 3~4주 뒤에.

**Fed 축 가중치 1:1:1 근거** — VIX 상관이 전부. 다중 타깃(S&P·Δ10y)에서도 1:1:1이 1위지만,
근거의 축 자체가 교수님이 보류시킨 시장지표. 아래 질문 1과 연결.

**뉴스 수집량** — 8월 들어 하루 3~10건까지 떨어져서 사이트가 거의 다 "낮음"이었음.
수집 상한(40쪽→80쪽)과 API 질의문을 고쳤고 실행당 신규 7건→16건까지 올라옴.
아직 15건 문턱 근처. 효과 확정은 며칠 더 봐야 함.

## 여쭐 것

1. **VIX 표시 여부** — Fed 축 가중치 근거가 보류된 지표에 서 있음
2. **키워드 조정** — ① 의장 이름을 F그룹에도 인정 ② M그룹에 `FOMC`·`minutes`·`rate hike` 추가 검토
   ③ `inflation`은 신중(정밀도 급락 우려)
3. **설명력 분석 설계 방향**

## 남은 일 — 설명력 분석

교수님 마지막 항목 = 조교님 분석 설계 지적. **관련 코드 아직 하나도 없음.**

- 조건부 빈도표
- 다항 로지스틱 회귀 (금리 결정이 인상/동결/인하 3분류라 OLS 부적절)
- 확장 윈도우 OOS (시계열이라 무작위 분할 금지)
- 견고성 검사 5종
- 2Y 국채는 창을 더 짧게
- R² 낮아도 실패 아님 — 해석 주의 문구 필요

교수님 답변 없이 시작 가능한 순서: 조건부 빈도표 → 다항 로지스틱 → 확장 윈도우 → 견고성

## 팀 참고

- **모델 학습 방식은 공개 문서에 쓰지 말 것** — README에서 제거함, 다시 안 들어가게 주의
- **키워드 필터는 교수님 지정 사항** — 기사 부족해도 임의로 넓히지 않음.
  같은 필터로 더 깊이 수집하는 쪽으로 해결함
- **브랜치 정리됨** — 원격에 `main`, `deploy/streamlit-dashboard`만 남음.
  각자 `git fetch --prune` 한 번

## 깃허브

열린 PR 없음 / 미푸시 커밋 없음 / 미커밋 변경 없음

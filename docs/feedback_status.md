# 피드백 반영 현황 (2026-07 ~ 2026-08)

> 지도교수·담당조교 피드백을 어디까지 반영했는지 한 곳에 모은다.
> 항목별 상세는 각 행의 링크 문서에 있고, 여기서는 **무엇을 · 왜 그렇게 · 어디까지** 만 본다.
> 최종 갱신 2026-08-26 · 기준 커밋 `d31be91` (main) / `4612dba` (deploy)

## 한눈에

| 출처 | 완료 | 진행/대기 | 미착수 |
|---|---|---|---|
| 지도교수 (2026-07) | 6 | 2 | 1 |
| 담당조교 (2026-08) | 4 | 1 | 2 |

**미착수 중 가장 큰 것은 설명력 분석**이다 — 교수님의 마지막 항목과 조교님의 분석
설계 지적이 같은 덩어리다. 나머지는 대부분 닫혔다.

---

## 1. 지도교수 피드백 (2026-07)

### ✅ 완료

**① 자체 라벨링 제거 · 원본 FinBERT(T=1) 복귀**

4인이 나눠 수행하던 문장 라벨링과, 그 라벨로 맞춘 온도보정(T=3.1)을 전부 걷어냈다.
*"기존 FinBERT가 정확하니 자체 보정은 불필요"* 라는 판단에 따른 것.
→ `8240208`, `b3d2e4b`. 온도는 `FINBERT_TEMPERATURE=1.0` 기본값으로 고정.

라벨링 산출물(정답지·워크시트·κ 문서)은 지우지 않고 git 이력에 남겼다 — 되돌릴
필요가 생기면 복원할 수 있고, 없앤 과정 자체가 기록이다.

**② 뉴스 선정을 지정 키워드 세트로 교체 (F그룹 ∧ M그룹)**

WSJ 과거 수집(2000~2021)에 쓴 것과 **같은 세트**를 라이브 수집에도 적용해
과거·현재 기준을 일치시켰다.
→ `bfb4e29`, 구현은 `engine/news_scrape.py::is_relevant`

```
F그룹 : "federal reserve" | fed                     (연준 자체 — 1개 이상 필수)
M그룹 : 정책·도구 27개 + 의장 성 + 국내외 중앙은행    (1개 이상 필수)
        money supply / open market operation / quantitative easing /
        monetary polic* / fed funds rate / overnight lending rate /
        interest rate / lender of last resort / discount window /
        central bank / fed chair(man) /
        bernanke / volcker / greenspan / yellen / powell / warsh /
        ECB / bank of england / bank of japan / bank of china /
        bundesbank / bank of france / bank of italy
```

**③ 회의록(minutes) 축 추가**

2000~2026 전 구간 214회의 수집·지수화 완료. 성명문·기자회견과 **동일한 공식**
(`conf_weighted`)을 쓰고, 섹션별 톤도 따로 낸다.
→ `adb7a0a`(백필), `8f423f5`(지수화), `8fc1e8d`(자동화 연결)

수집기를 만들며 팀 정리본(2000~2009)도 같은 형식으로 이관했고, 파서에서 두 가지를
고쳤다 — `<blockquote>`·`<ul>` 을 읽지 않아 본문 최대 15%가 누락되던 것, 표결 행이
섹션 제목으로 잡히던 것.

**④ 성명문·회의록·기자회견 분리 분석**

세 축을 각각 FinBERT로 돌려 별도 지수를 낸 뒤 결합한다. 사이트에도 축별 페이지가
따로 있다. 축 간 상관 0.34~0.67 — 서로 다른 정보를 담고 있음을 확인.
→ `docs/fed_weights.md`

**⑤ 크롤링 금지 → API 사용**

Marketaux API로 전환. HTML 크롤링은 하지 않는다.
→ `engine/news_scrape.py`

**⑥ 시장 비교·VIX 보류**

지시대로 보류 상태를 유지한다. 사이트에도 전면에 내세우지 않는다.

> ⚠️ 다만 **Fed 축 내부 가중치(1:1:1)의 근거가 전부 VIX 상관에 서 있다**
> (`docs/fed_weights.md`). 보류된 기준으로 설계 결정을 정당화한 셈이라,
> 이 부분은 교수님 확인이 필요하다. → 아래 §3 질문 1

### ⏳ 확인 대기

**⑦ 키워드 조정 여부**

탈락 기사 감사에서 **놓침률 27.3%** 가 나왔다. 특히 **FOMC 회의록 보도 4건이 전부
탈락**했는데, 회의록은 이 프로젝트의 핵심 자료다.
→ `docs/filter_audit.md`

원인은 두 가지다. `minutes`·`FOMC`·`rate hike` 가 M그룹에 없고, 의장 이름이 M그룹에만
있어서 *"파월이 …"* 처럼 의장 이름만 나온 기사가 F 조건을 못 채운다.

**키워드는 교수님 지정 사항이므로 임의로 바꾸지 않았다.** 아래 §3 질문 2 로 여쭙는다.

### ❌ 미착수

**⑧ 설명력 분석** — 아래 §2 의 조교 지적과 같은 항목. §4 참조.

---

## 2. 담당조교 피드백 (2026-08)

질문 5·6 의 답변 원문과 설계 근거는 `docs/ta_feedback_2026-08.md` 에 따로 있다.

### ✅ 완료

**질문 5 — 표본이 적은 날의 신호 발동 (신뢰도 게이트)**

*문제*: 일별 지수는 부트스트랩 95% CI를 계산해 표시하면서, 정작 신호 등급은 CI를
무시하고 점추정치만으로 발동했다. **기사 6건인 날에도 "주의"가 나갔다.**

*조교 답변*: A안(게이팅)·B안(표시)의 절충 — **측정은 그대로 두고 경보만 게이팅.**

*반영*: `agents/graph.py::strategy_node` 에 `news_signals.confident()` 배선.
기사 15건 미만이거나 CI 폭 0.60 초과면 `주의`/`경고` 를 **`⚪ 관망`** 으로 낮춘다.
지수 값 자체는 건드리지 않는다 — 기사 1~2건인 날 지수가 크게 튀지만(실측 +7.09),
값을 빼면 시계열에 구멍이 생긴다. 발동 목록도 기록용으로 남긴다.
→ `1066adf`, `13bb420`, 머지 `fad59b3`

**질문 6 — 회의록 3주 지연 처리 (속보치/확정판 이원화)**

*문제*: Fed 축을 3성분 1:1:1 로 확장하기로 검증했으나(`docs/fed_weights.md`),
회의록은 회의 3주 뒤에 공개된다. 회의 당일엔 성명문(+기자회견)뿐이다.

*조교 답변*: 절충안(속보치/확정판 분리). 단 **과거 실시간 값을 덮어쓰면 안 된다.**

*반영*:
- `analysis/headline.py::combine_fed_axes()` — 가용한 성분끼리만 z-결합.
  회의 당일엔 자동으로 1~2축, 회의록 도착 후 3축.
- `meetings` 테이블에 `fed_composite_realtime`(확정 전까지만 갱신) /
  `fed_composite_final`(3축 완성 시 **단 1회만** 기록, 이후 불변) 분리 저장.
- `daily_signals.csv` 의 `grade`/`index` 는 재방문해도 **절대 수정하지 않는다.**
  확정판은 `grade_final`/`index_final`/`finalized_at` 에만 추가 기록.
- 부수 수정: `combine()` 이 이미 z-표준화된 값을 다시 표준화해 지수가 부풀던 버그.

*검증*: 2026-06-17 회의 실행 — 속보치 2.0734 보존, 확정판 1.007 추가 기록 확인.

**신뢰도 UI 단순화**

*조교 답변*: CI·확신도는 유지하되 화면은 **높음·보통·낮음** 3단계로, 상세 수치는
`상세보기` 로 뺄 것.

*반영*: `dashboard-web/src/lib/data.js::confidenceLevel()`.
게이트와 **같은 임계값**(15건·0.60)을 공유해 사이트와 신호가 어긋날 수 없게 했다.
→ `8c11d0c`

**과거 실시간 값 덮어쓰기 금지 (원칙)**

두 겹으로 지킨다. 성격이 다르다.

| | 무엇 | 용도 |
|---|---|---|
| `analysis/vintage.py` | 축이 하나씩 도착할 때마다의 **전체 이력** (`as_of` 포함) | 분석에서 as_of 재구성 |
| 속보치/확정판 | 그 날짜의 **최종 상태 2개** | 사이트 표시 |

→ `dd7b93a`(vintage 도입), 백필로 과거 231행 복원

### ⏳ 진행 중

**뉴스는 넓히지 말고 탈락분을 감사할 것**

감사 1차 완료(놓침률 27.3%) → `docs/filter_audit.md`. 다만 표본이 **33건**뿐이라
문서에 *"기간을 늘려 재확인 필요"* 라고 명시해뒀다.

재감사를 못 하고 있던 이유가 2026-08-26 에 밝혀졌다 — `data/news/` 가 gitignore 인데
워크플로 커밋 목록에 `rejected_news.csv` 가 빠져 있어서, **프로덕션 탈락 기사가 한
번도 보존된 적이 없었다.** 하루 164건이 러너와 함께 증발하고 있었다.
1차 감사의 33건은 로컬 실행분이었다.
→ `d31be91` 에서 수정. 이제 매일 쌓인다.

### ❌ 미착수

**알림(alert) 기능 확장** — 조교님이 좋다고 하신 항목. 선택 사항이라 뒤로 미뤘다.

**분석 설계 (4-1 재구성 외)** — §4 참조.

---

## 3. 여쭐 내용

**질문 1 — VIX 표시 여부**

Fed 축 내부 가중치를 1:1:1 로 정한 근거가 **VIX 상관 하나에 서 있다**
(전 구간 -0.418 → -0.577, 홀드아웃 3분할 전부 통과 — `docs/fed_weights.md` §2,
그림 `docs/figures/comparison/fed_weights.png`).

다중 타깃(S&P·Δ10y)에서도 1:1:1 이 1위라 VIX 하나에 맞춘 것은 아니지만, **근거의 축
자체가 보류하신 시장지표**다. 사이트에 VIX 를 어디까지 노출할지, 그리고 이 근거를
그대로 써도 되는지 확인이 필요하다.

**질문 2 — 키워드 조정**

감사 결과(§1-⑦)를 근거로 세 가지를 여쭙는다.

1. **의장 이름을 F그룹에도 인정** — 의장 이름은 연준을 특정하는 표현이다. 규칙을
   바꾸는 게 아니라 *"F = 연준을 가리키는 표현"* 이라는 원래 취지에 맞추는 것.
2. **M그룹에 `FOMC`·`minutes`·`rate hike`·`rate cut` 추가 검토** — 통화정책 자체를
   가리키는 표현인데 빠져 있다. FOMC 회의록 보도 전량 탈락의 직접 원인.
3. **`inflation` 추가는 신중** — 물가 기사 전반이 들어와 정밀도가 크게 떨어질 수 있다.

**질문 3 — 설명력 분석 설계** — §4 의 방향으로 진행해도 되는지.

---

## 4. 남은 가장 큰 것 — 설명력 분석

교수님의 마지막 항목과 조교님의 분석 설계 지적이 같은 덩어리다. 저장소를 뒤졌으나
**관련 코드가 아직 하나도 없다.**

조교님이 짚어주신 것:

| | 지적 |
|---|---|
| 4-1 재구성 | 금리 결정은 인상·동결·인하 **3분류**라 OLS 가 부적절 → **다항 로지스틱 회귀** |
| R² | 사회과학 자료에서 낮은 R² 를 실패로 읽지 말 것. 해석에 주의 문구 필요 |
| 조건부 빈도표 | 좋은 접근이니 살릴 것 |
| 2Y 국채 | 창을 더 짧게 |
| 표본 분할 | **무작위 분할 금지** — 시계열이므로 확장 윈도우(expanding window) |
| 견고성 | 5종 검사 |

착수 순서는 교수님 답변과 무관하게 시작할 수 있는 것부터:
조건부 빈도표 → 다항 로지스틱 → 확장 윈도우 OOS → 견고성 검사.

---

## 5. 반영하며 지킨 원칙

문서를 나중에 읽을 사람을 위해, 판단이 갈렸던 지점만 남긴다.

**측정과 경보를 분리한다.** 신뢰도가 낮다고 지수를 지우지 않는다. 지우면 시계열에
구멍이 생기고, 나중에 "그날 값이 왜 없지"를 설명할 수 없다. 값은 남기고 등급만 낮춘다.

**과거에 보여준 값은 사후에 바꾸지 않는다.** 자료가 시차를 두고 도착하므로(회의록 3주
뒤), 나중 정보로 과거 기록을 고치면 설명력·예측력이 실제보다 좋아 보인다.

**임계값은 데이터로 정하되, 데이터가 오염됐으면 기다린다.** 신호 임계값(θ)은 분포
분위수로 보정했다(`docs/signal_calibration.md`). 신뢰도 게이트의 15건·0.60 은 아직
**잠정값**이다 — 지금 보정하면 수집이 깨져 있던 기간이 표본에 섞여 버그를 임계값에
박아 넣게 된다. 정상 수집이 3~4주 쌓인 뒤 같은 절차로 보정한다.

**가중치는 튜닝하지 않는다.** 사전에 고정한 후보만 비교한다. 소수점 최적화는 표본에
과적합돼 표본 외에서 오히려 나빠진다(1/N 규칙).

**지도교수 지정 사항은 임의로 바꾸지 않는다.** 키워드에 문제를 발견해도 근거를 만들어
보고하고 결정을 받는다. 수집량이 부족했을 때도 필터를 넓히는 대신, **같은 필터로 더
깊이 표본추출**하는 쪽으로 풀었다(모집단은 그대로, 표본만 확대).

---

## 6. 반영 커밋

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 08-26 | `d31be91` | 페이지 실패 건너뛰기 · 질의문 F 필수 · 탈락기록 보존 |
| 08-25 | `fad59b3` | **PR #4 머지** — 조교 질문 5·6 |
| 08-25 | `a0d9d79` | 수집량 40쪽→80쪽 · 수집 진단 로그 |
| 08-25 | `c496f98` | Fed 가중치 1:1:1 근거 그림 |
| 08-25 | `06ec085` | 뉴스 필터 감사 (놓침률 27.3%) |
| 08-25 | `8c11d0c` | 신뢰도 3단계 UI + 상세보기 |
| 08-25 | `13bb420` | 신뢰도 게이트 연결 |
| 08-25 | `dd7b93a` | 시점 기록(vintage) 도입 |
| 08-25 | `86a213c` | 공개 문서 정리 |
| 08-24 | `1066adf` · `5ca0401` | 게이트 배선 + Fed 3축 결합 (PR #4 본체) |
| 08-14 | `22a2a53` | 통합 가중치 근거 문서화 |
| 08-05 | `8fc1e8d` | 3축 자동화 완성 (미완성 회의 재방문) |
| 08-04 | `adb7a0a` | 회의록 전 구간 (2000~2026, 214회의) |
| 08-03 | `bfb4e29` | 뉴스 선정을 지정 키워드 세트로 교체 |
| 08-03 | `8240208` · `b3d2e4b` | 자체 라벨링·보정 제거, T=1 복귀 |

열린 PR 없음. 원격 브랜치는 `main` · `deploy/streamlit-dashboard` 둘뿐.

## 관련 문서

- `docs/ta_feedback_2026-08.md` — 조교 질문 5·6 답변 원문과 설계 근거
- `docs/filter_audit.md` — 뉴스 필터 감사 (놓침률·원인·조치안)
- `docs/fed_weights.md` — News:Fed 1:1, Fed 내부 1:1:1 검증
- `docs/signal_calibration.md` — 신호 임계값(θ) 분포 기반 보정
- `docs/TEAM_GUIDE.md` — 팀 작업 가이드

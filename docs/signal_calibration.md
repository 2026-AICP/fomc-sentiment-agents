# 신호 임계값(θ) 보정 · 민감도 분석

> 데이터 기반 θ 설정 + 민감도 분석으로 "자의성" 논란을 차단하는 근거 자료.
> (2026-07-03, 59개 회의 · 코드: `analysis/signals.py` `Thresholds`)

> ## ⚠️ 이 문서의 분포표는 더 이상 현재 데이터가 아니다 (2026-09-11 추가)
>
> 아래 §2 표와 그에 근거한 §3 의 θ 는 **온도보정(T=3.1) 엔진 시절** 값이다.
> 2026-08-03(`8240208`)에 원본 FinBERT(T=1)로 전환하면서 **톤 척도가 약 2.2배
> 넓어졌는데 θ 는 따라가지 않았다.**
>
> | | 이 문서 (T=3.1) | 실측 (T=1, 성명문 214건) |
> |---|---|---|
> | \|톤\| p25 | 0.056 | **0.112** |
> | \|톤\| p50 | 0.100 | **0.240** |
> | \|톤\| max | 0.331 | **0.728** |
> | \|Δ톤\| p85 | 0.097 | **0.228** |
>
> **결과: `theta_shift = 0.10` 의 실제 발동률이 45.5% 다** (설계 의도 ~15%).
> `theta_t = 0.05` 도 현재 p25(0.112)의 절반이다.
> 시장 척도 θ(`theta_m` 0.80 · `theta_vix` 1.00 · `theta_rate` 0.05)는 톤과
> 무관하므로 영향이 없다.
>
> **본문은 고치지 않는다.** 2026-07-03 시점에는 맞는 계산이었고, 그 시점 기록이
> 남아야 "결과를 보고 θ 를 고른 게 아니다"가 성립한다. 재보정은 새 분포에서
> 같은 방법(분위수)으로 다시 하고, 이 문서가 아니라 재보정 문서에 기록한다.
>
> **범위 주의** — `analysis/signals.py` 에는 θ 세트가 둘이다.
> `COMBINED_THRESHOLDS`(0.95 / 0.22, 일별 알림 — `agents/graph.py`)와
> `DEFAULT_THRESHOLDS`(0.10 / 0.05, 회의별 신호 — `analysis/export_dashboard.py`
> 가 θ 를 넘기지 않아 이 값을 쓴다). **둘 다 어긋나 있고 둘 다 사용자에게
> 보인다.** 남은 결정 B-1 의 범위는 두 세트 전부다.

## 1. 왜 — "근거"의 3층위

```
① 신호가 발동한다 (θ 적용)        ← 근거 아님. 규칙 적용일 뿐
② θ가 자의적이지 않다 (데이터 기반) ← "체리피킹 아님" 방어
③ 신호가 실제로 의미있다 (검증)     ← ★ 진짜 근거 (상관·백테스트)
```

- 표본이 작을 때(59개) θ를 "결과 좋게" 최적화하면 **과최적화(overfitting)** → 오히려 약점.
- 따라서 **원칙 기반 θ(분포 분위수) + 민감도 분석**이 정답. "완벽한 θ 하나"보다 "θ에 안 흔들림"이 강한 근거.

## 2. 실제 분포 (59개 회의, 절대값)

| | p25 | p50 | p75 | max |
|---|---|---|---|---|
| \|톤\| | 0.056 | 0.100 | 0.150 | 0.331 |
| \|S&P %\| | 0.375 | 0.843 | 1.239 | 9.38 |
| \|VIX 변화\| | 0.410 | 0.900 | 1.910 | 13.2 |
| \|Δ톤(급변)\| | 0.014 | 0.036 | 0.075 | 0.345 |

→ 톤은 ±0.15에 몰림(성명문이 중립적). FOMC일 시장 움직임 중앙값 0.84%.

## 3. 데이터 기반 θ (잠정값 → 보정)

| θ | 이전(잠정) | 보정 | 근거 |
|---|---|---|---|
| `theta_t` | 0.05 | **0.05 (유지)** | \|톤\| p25 ≈ 0.056 과 일치 |
| `theta_vix` | 1.00 | **1.00 (유지)** | \|VIX변화\| p50 ≈ 0.9 와 근접 |
| `theta_m` | 0.30 | **0.80** | \|S&P%\| p50 ≈ 0.84 (이전 0.30은 과소 → 괴리 남발) |
| `theta_shift` | 0.20 | **0.10** | \|Δ톤\| p85 ≈ 0.097 (상위 ~15%, 발동 14%). 0.20 과대(A 잠듦)·0.07 과빈 |

**효과 (59개 회의):**
```
divergence  14 → 9   (경고 더 선별적, 강한 스트레스만)
tone_shift   3 → 8   (잠자던 A 활성화, 상위~15% 톤변화만)
tone_vs_vix  9 → 9   (유지)
등급:  경고 14→9 · 주의 2→9 · 정합 29→27 · 중립 14→14
```

## 4. 민감도 분석 ⭐ (핵심 근거)

θ 배율별 '괴리' 신호가 뜬 회의:

| θ 배율 | 괴리 | 회의 |
|---|---|---|
| ×0.5 | 16 | 2000·2021·2022(6)·2023(3)·2024·2025 |
| ×1.0 | 8 | 2000-02·2000-06 · 2021-12 · 2022-05·11·12 · 2023-09 · 2024-07 |
| ×1.5 | 2 | 2023-09-20 · 2024-07-31 |
| ×2.0 | 0 | — |

→ **θ를 어떻게 잡든 괴리는 일관되게 위기·긴축기에 집중** (2000 닷컴 · 2022-23 급속 인상기 · 2024 전환점).
→ 가장 강한 괴리(2023-09·2024-07)는 **θ를 2배로 올려도 유지**.
→ 즉 **"괴리 = 스트레스 시점" 결론은 θ에 흔들리지 않음** = 자의성 논란 차단.
(개수는 θ에 따라 변하지만, 성격·집중 시점은 불변.)

## 5. 포스터·보고서 문장 (방어 가능)

> "임계값 θ는 59개 회의의 톤·시장 반응 분포(분위수)로 설정. **민감도 분석: θ 배율 0.5~2.0 전 구간에서 괴리 신호는 일관되게 위기·긴축기에 집중**(2000 닷컴·2022-23 인상기·2024). 가장 강한 괴리는 θ 2배에도 유지. 통합 감성지수는 VIX와 −0.52 상관. ※ 상관 ≠ 인과, 예측 아닌 경향."

## 6. 한계 · 재현

- 표본 59개 → 보정 통계력 제한. θ는 **원칙 기반(분위수)**이지 결과 최적화 아님(과최적화 회피).
- 재보정: 데이터 쌓이면 `analysis/signals.py` `Thresholds` 한 곳만 수정.
- 재현: `python3` 로 `load_series` → 분포·민감도 재계산 (CPU, 즉시).
